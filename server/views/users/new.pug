extends ../layouts/application.pug

block header
  = t('views.users.new.signUp')

block content
  +formFor()(action=route('users') method='post')
    +input(usersForm, 'firstName', errors)(type="text")
    +input(usersForm, 'lastName', errors)(type="text")
    +input(usersForm, 'email', errors)(type="text")
    +input(usersForm, 'password', errors)(type="password")
    +submit(usersForm)(value=t('views.users.new.submit'))
